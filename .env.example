# ─────────────────────────────────────────────────────────────────
# Distribution Tracker v2.0.0 — Configuration
# Copier ce fichier en .env et adapter les valeurs
# ─────────────────────────────────────────────────────────────────

# Port d'écoute du serveur
PORT=3000

# ── Compte admin initial ───────────────────────────────────────────
# Utilisé UNE SEULE FOIS au premier démarrage si aucun admin n'existe.
# Changer le mot de passe après la première connexion.
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=admin123

# ── Sécurité JWT ───────────────────────────────────────────────────
# Clé secrète pour signer les tokens JWT.
# IMPORTANT : générer une valeur aléatoire en production :
#   openssl rand -hex 32
JWT_SECRET=changeme-set-a-random-secret-in-production

# ── URL publique ───────────────────────────────────────────────────
# URL complète de l'application, sans slash final.
# Utilisée pour générer les QR codes et les liens de réinitialisation.
# Laisser vide en développement local (auto-détecté).
BASE_URL=http://localhost:3000

# ── Stockage ───────────────────────────────────────────────────────
# Répertoire de la base SQLite dans le container Docker.
# Monter un volume ou bind mount sur ce chemin pour la persistance.
DATA_DIR=/data
